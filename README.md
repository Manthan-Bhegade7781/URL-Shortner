# URL-Shortner
A full-stack URL Shortener built with Node.js, Express.js, MongoDB, EJS, and JavaScript. It allows users to create and manage shortened URLs securely using JWT authentication and authorization. The application uses the shortid package for unique URL generation and MongoDB for efficient data storage.
